<Window x:Class="tp3_prog.PartyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:tp3_prog"
        mc:Ignorable="d"
        Title="" 
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStyle="ToolWindow">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0"
                   FontWeight="Bold" 
                   Background="Black" Foreground="White" 
                   TextAlignment="Center" Padding="10">Party</TextBlock>
        
        <Button x:Name="ButtonUnequip" 
                Grid.Row="0" Grid.Column="1"
                Padding="20 5" 
                >Unequip</Button>

        <StackPanel x:Name="PanelPartyMembers" 
                    Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" />
        <Button x:Name="RefreshData" Padding="20 5">Refresh Data</Button>

    </Grid>
    
</Window>
